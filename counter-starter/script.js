function add(step) {

    // Get DOM Element

    let counter = document.getElementById('number');
    
    // Add 1 to the current value

    let newValue = parseInt(counter.innerHTML) + step;

    // Re-assign the value to the DOM element
    counter.innerHTML = newValue

}

    console.log ("Yes we are working", newValue);


// Subtraction function

function subtract(step) {

    // Get DOM Element

    let counter = document.getElementById('number');
    
    // Add 1 to the current value

    let newValue = parseInt(counter.innerHTML) - step;

    // Re-assign the value to the DOM element
    counter.innerHTML = newValue

}

    console.log ("Yes we are working", newValue);


// Multiplication function

function multiplicate(step) {

    // Get DOM Element

    let counter = document.getElementById('number');
    
    // Add 1 to the current value

    let newValue = parseInt(counter.innerHTML) * step;

    // Re-assign the value to the DOM element
    counter.innerHTML = newValue

}

    console.log ("Yes we are working", newValue);


    // Division function

function divide(step) {

    // Get DOM Element

    let counter = document.getElementById('number');
    
    // Add 1 to the current value

    let newValue = parseInt(counter.innerHTML) / step;

    // Re-assign the value to the DOM element
    counter.innerHTML = newValue

}

    console.log ("Yes we are working", newValue);


    // Input Text Object

function inputText() {

   